# Frontend environment configuration for AI Task Manager
# Copy to `.env` (or configure in your hosting provider) and update values per environment.

# URL of the deployed backend API (Railway, Render, etc.).
# Example: https://ai-task-manager-production.up.railway.app
VITE_BACKEND_BASE_URL=https://your-backend.up.railway.app
