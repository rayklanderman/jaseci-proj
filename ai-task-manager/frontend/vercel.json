{
  "version": 2,
  "outputDirectory": "dist",
  "buildCommand": "yarn install && yarn build",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
