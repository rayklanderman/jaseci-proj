# Backend environment configuration for AI Task Manager
# Copy this file to `.env` (or configure directly in your platform) and update the values.

# Google Gemini API key used by JacMachine/byllm for AI brief generation.
GEMINI_API_KEY=replace-with-your-gemini-api-key

# Database connection string. SQLite works locally; use PostgreSQL for Railway.
# Examples:
#   sqlite:///./task_manager.db
#   postgresql+psycopg://user:password@host:port/database
DATABASE_URL=postgresql+psycopg://USERNAME:PASSWORD@HOST:PORT/DATABASE

# Comma-separated list of allowed origins for CORS.
# Include your Vercel and custom domains.
FRONTEND_ORIGINS=https://your-frontend.vercel.app,https://www.yourdomain.com

# Optional: override the port when running locally.
# PORT=8000
