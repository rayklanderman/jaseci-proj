{
  "version": 2,
  "outputDirectory": "frontend/dist",
  "buildCommand": "cd frontend && yarn install && yarn build",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
